/*
var http = require('http');

http.createServer(function(req, res){
  res.end("\tHello World! Welcome to my website\n\n");
  res.end("\tInstalar os pacotes: npm install express --save");
  res.end("\tInstalar os pacotes: npm install nodemon -g");
  res.end("\tAcesso ao MySQL: mysql -h localhost -u root -p");
  res.end("\tInstalar os pacotes: npm install --save sequelize");
  res.end("\tInstalar os pacotes: npm install --save bcryptjs");
  res.end("\tInstalar os pacotes: npm install --save passport");
  res.end("\tInstalar os pacotes: npm install --save passport-local");

		Instalando pacotes vue
  res.end("\tInstalar os pacotes: npm install -g @vue/cli");
  res.end("\tInstalar os pacotes: vue create projeto-simples");  
  res.end("\tInstalar os pacotes: npm run build");  
  res.end("\tInstalar os pacotes: vue add electron-builder");
  res.end("\tInstalar os pacotes: npm run electron:serve");  
  res.end("\tInstalar os pacotes: vue add vuetify"); 

        Setting of Progeto Final
  res.end("\tInstalar os pacotes: npm i -g knex");  
  res.end("\tInstalar os pacotes: npm install pg --save");
 res.end("\tInstalar os pacotes: npm install --save express-handlebars");  
  res.end("\tInstalar os pacotes: npm install --save body-parser");  
  res.end("\tInstalar os pacotes: npm install --save express-handlebars");  
  res.end("\tInstalar os pacotes: npm install --save body-parser");
 res.end("\tInstalar os pacotes: npm install --save express-handlebars");  
  res.end("\tInstalar os pacotes: npm install --save body-parser");  
  res.end("\tInstalar os pacotes: npm install --save express-handlebars");  
  res.end("\tInstalar os pacotes: npm install --save body-parser");  
  res.end("\tInstalar os pacotes: npm install --save express-handlebars");  
  res.end("\tInstalar os pacotes: npm install --save body-parser");  
  res.end("\tInstalar os pacotes: npm install --save express-handlebars");  
  res.end("\tInstalar os pacotes: npm install --save body-parser");  
   res.end("\tInstalar os pacotes: npm install --save express-handlebars");  
  res.end("\tInstalar os pacotes: npm install --save body-parser");  
  res.end("\tInstalar os pacotes: npm install --save express-handlebars");  
  res.end("\tInstalar os pacotes: npm install --save body-parser");
  
  
  res.end("\tInstalar os pacotes: npm install --save sequelize");
  res.end("\tInstalar os pacotes: npm install --save sequelize");
  res.end("\tInstalar os pacotes: npm install --save sequelize");
  res.end("\tInstalar os pacotes: npm install --save sequelize");
  res.end("\tInstalar os pacotes: npm install --save sequelize");
  res.end("\tAcesso ao MySQL: mysql -h localhost -u root -p");
  res.end("\tAcesso ao MySQL: mysql -h localhost -u root -p");
  res.end("\tAcesso ao MySQL: mysql -h localhost -u root -p");
  res.end("\tAcesso ao MySQL: mysql -h localhost -u root -p");
  res.end("\tAcesso ao MySQL: mysql -h localhost -u root -p");
  res.end("\tAcesso ao MySQL: mysql -h localhost -u root -p");
  res.end("\tAcesso ao MySQL: mysql -h localhost -u root -p");
  res.end("\tAcesso ao MySQL: mysql -h localhost -u root -p");
  res.end("\tAcesso ao MySQL: mysql -h localhost -u root -p");
  res.end("\tAcesso ao MySQL: mysql -h localhost -u root -p");

    {
    "name": "Juliano",
    "email": "juliano@agmail.com.br",
    "password": "123",
    "confirmPassword": "123",
    "admin": true
    "admin": false
}


*/
